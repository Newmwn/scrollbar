/**
 * Enum with the current devices that the getCurrentDevice() function retrieves
 */
export enum DevicesEnum {
  Android = "Android",
  IOS = "IOS",
  Other = "Other",
}

export type DeviceOptions = "Android" | "IOS" | "Other";
